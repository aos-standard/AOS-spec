# Minimal AOS Hook — Python Example

This example shows the smallest practical PreToolUse hook that blocks writes into Oracle-class paths before filesystem side effects occur.

See [`hook.py`](./hook.py). Input payload shape matches the comment header inside that file.

For a slightly richer variant (additional Oracle patterns), see [`../hook_pretooluse.py`](../hook_pretooluse.py).
